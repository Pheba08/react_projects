import React from "react";
import { useNavigate } from "react-router-dom";

const FiftthStdPack = () => {
  const navigate = useNavigate();
  return (
    <div>
        
      <p>hi</p>
    </div>
  );
};

export default FiftthStdPack;
